import org.zenframework.z8.lang.Object;

[native "org.zenframework.z8.server.base.query.Period"]
class Period extends Object
{
	public PeriodType type;
	public date start;
	public date finish;
	
	static public Period create(PeriodType type);
	static public Period create(PeriodType type, date start);
	static public Period create(date start, date finish);
	
	public operator string();
	
	public bool isEmpty();

	public Period intersect(Period period);
}
